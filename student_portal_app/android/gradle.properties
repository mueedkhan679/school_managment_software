org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# All dependencies are already downloaded and cached locally; running Gradle
# in offline mode avoids slow/failing network resolution during configuration.
# (Configuration cache is NOT enabled: the Flutter DependencyVersionChecker
# MinSdkCheck task is incompatible with config-cache storage on Java 21.)
org.gradle.offline=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
